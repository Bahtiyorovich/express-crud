ushbu paketlarni o'rnating

Ekspres — Node.js ramkasi: npm install express
Sequelize - PostgreSQL uchun ob'ektga aloqador xaritalash vositasi: 
npm install --save sequelize
 Pg va pg-hstore — pg — Node.js uchun PostgreSQL mijozi va pg-hstore — JSON maʼlumotlarini hstore formatiga seriyalashtirish va seriyadan chiqarish uchun tugun paketi:
 npm install --save pg pg-hstore
Nodemon - Fayl o'zgarishlari aniqlanganda tugun dasturini avtomatik qayta ishga tushirish:
npm install –save-dev nodemon
Bcrypt - parolni xeshlash uchun => npm install bcrypt
Dotenv =>Atrof-muhit o'zgaruvchisiga kirish uchun => npm install dotenv
JSON Web Token => Tokenlarni yaratish uchun => npm install jsonwebtoken
CookieParser => Cookie-fayllarni sozlash uchun => npm install cookie-parser

O'rnatishni tugatganingizdan so'ng server.js faylini yarating va shu kabi skriptlarga start qo'shish orqali package.json faylingizni o'zgartiring .

Ekspres - tugun uchun tezkor, noaniq, minimalist veb-ramka .

Sequelize — Sequelize so'rovlarni amalga oshirish uchun ishlatiladigan Postgres , MySQL , MariaDB , SQLite , DB2 , Microsoft SQL Server va Snowflake uchun ishlatish uchun qulay va va'daga asoslangan Node.js ORM vositasidir .

Nodemailer — Nodemailer oson elektron pochta xabarlarini yuborish imkonini beruvchi Node.js ilovalari uchun moduldir.

pg & pg-hstore — pg — bu Node.js uchun bloklanmagan PostgreSQL mijozi va pg-hstore — JSON maʼlumotlarini maʼlumotlar bazasi uchun ishlatiladigan hstore formatiga seriyalashtirish va seriyadan chiqarish uchun tugun paketidir.

Jsonwebtoken - Tokenlarni yaratish uchun

Cookie-parser - Cookie-fayllarni sozlash uchun

Helmet.js — Dubulg‘a turli HTTP sarlavhalarini o‘rnatish orqali Express ilovalaringizni himoya qilishga yordam beradi.

Crypto.js — Tasodifiy satrlarni yaratish uchun

Bcrypt - parollarni hashlashda yordam beradigan kutubxona.

Nodemon — Nodemon — katalogdagi fayl oʻzgarishlari aniqlanganda tugun ilovasini avtomatik ravishda qayta ishga tushirish orqali Node.js asosidagi ilovalarni ishlab chiqishga yordam beruvchi vositadir.

Dotenv — Dotenv nolga bogʻliqlik moduli boʻlib, muhit oʻzgaruvchilarini .env faylidan process.env ga yuklaydi.

O'rnatishni tugatganingizdan so'ng server.js faylini yarating va shu tarzda skriptga start qo'shish uchun package.json faylingizni o'zgartiring .


